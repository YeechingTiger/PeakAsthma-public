angular.module('pa.firebase', ['ui.bootstrap','ui.router','ngAnimate']);
angular.module('pa.firebase').config(function($stateProvider) {

    /* Add New States Above */

}).run(function(pushNotificationService) {
  
});