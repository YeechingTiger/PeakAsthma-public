describe('pushNotificationService', function() {

  beforeEach(module('pa.firebase'));

  it('should ...', inject(function(pushNotificationService) {

    //expect(pushNotificationService.doSomething()).toEqual('something');

  }));

});
